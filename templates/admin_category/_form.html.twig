

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-lg btn-block" style="background-color: #AE9A88; color: #F1EFE7;">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}


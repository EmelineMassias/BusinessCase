<form method="post" action="{{ path('app_admin_vetements_delete', {'id': vetement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vetement.id) }}">
    <button style="color: #AE9A88 !important;" class="btn"><i class="fa-regular fa-trash-can"></i></button>
</form>
